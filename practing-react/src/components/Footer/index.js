import React from "react";
import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <span>Desenvolvido por Shigeru</span>
    </footer>
  );
};

export default Footer;
